name: hod
author: Team Siberia
data_pack:
  load:
    - src
resource_pack:
  load:
    - src
require:
  - bolt
  - bolt_expressions
pipeline:
  - mecha
  # - src.modules.make
output: build
meta:
  bolt:
    entrypoint:
      - hod:root